import 'package:flutter/material.dart';

class ThirtyDay extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Tab 30D Layout'),
      ),
    );
  }
}